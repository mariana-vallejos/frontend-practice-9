export type Book = {
    id: number,
    title: string,
    description: string,
    author: string,
    coverImage: string,
    publishedYear: number,
    genre: string,
    rating: number,
    pages: number,
    language: string,
    isbn: string
}